# Benchmark credentials — copy to .env and fill in your tokens.
# NEVER commit .env — it contains real credentials.

# Claude Code OAuth tokens (for claude-based benchmark agents)
CLAUDE_CODE_OAUTH_TOKEN=
CLAUDE_CODE_OAUTH_TOKEN_2=
CLAUDE_CODE_OAUTH_TOKEN_3=

# Claude identity (optional — for API fingerprinting)
CLAUDE_USER_ID=
CLAUDE_ACCOUNT_UUID=
CLAUDE_ORG_UUID=
