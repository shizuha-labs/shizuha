# PLAT-4701: locked agent-runtime cryptography identity.
# Unbounded `pip install cryptography` backtracked to source-only 2.6 on
# Python 3.12 (run-79), failing with ModuleNotFoundError: pkg_resources.
# Pin one reviewed release that publishes manylinux wheels for CPython 3.12
# (abi3) on amd64+arm64. Install with --only-binary=:all: so missing wheels
# fail closed instead of source-building.
# PLAT-4760: bumped 45.0.7 -> 49.0.0 to clear 7 high advisories on 45.0.7
# (CVE-2026-26007 / PYSEC-2026-2141, GHSA-537c-gmf6-5ccf [patched 48.0.1],
# GHSA-m959-cc7f-wv43, GHSA-p423-j2cm-9vmq, GHSA-r6ph-v2qm-q3c2, PYSEC-2026-35/36).
# PLAT-5806: 49.0.0 -> 50.0.0 clears GHSA-g6cj-pr64-35w5 / PYSEC-2026-3552.
# 50.0.0 publishes abi3 manylinux+musllinux wheels for CPython on amd64+arm64.
cryptography==50.0.0
