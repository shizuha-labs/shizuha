# Fleet plugin (not included)

This public tree ships only the no-op stub. The real Hive/k3s actuator
is an internal plugin on `shizuha-beta` / `hive-runtime`.

`shizuha` (TUI) and `shizuha up` (browser dashboard) do not need it.
